from fastapi import APIRouter

# Router para futuras rutas de autenticación (si es necesario)
router = APIRouter(tags=["auth"])

# Si se añaden más rutas de autenticación, las definirías aquí con el path completo.
